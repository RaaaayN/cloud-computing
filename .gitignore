__pycache__/
*.py[cod]
.pytest_cache/
venv/
samples/
# but keep the real ImageNet query images bundled with the loadtester package
!src/load_tester/samples/
!src/load_tester/samples/*.JPEG
results.csv
benchmarks/

# experiment outputs
*.csv
comparison_*.png
# but keep the committed campaign results (CSVs + figures) for the report
!experiments/results/
!experiments/results/*.csv
!experiments/results/*.png

# local planning / working-memory files (not part of the repo)
task_plan.md
findings.md
progress.md
